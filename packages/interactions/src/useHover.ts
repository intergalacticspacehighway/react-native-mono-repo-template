import { HoverProps } from '@react-aria/interactions';

export const useHover = (_props: HoverProps, _ref: any) => {
  console.log('ph mannn ');
  let params = {
    hoverProps: {},
    isHovered: false,
  };

  return params;
};
